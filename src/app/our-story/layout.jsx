export const metadata = {
  title: "Nassy & Franc",
  description: "A Story of Love",
};

export default function StoryLayout({children}) {
  return (
    <div>
      {children}
      
    </div>
  )
}
